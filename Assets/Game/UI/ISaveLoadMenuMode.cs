public interface ISaveLoadMenuMode
{
    /// <summary>
    /// Switches the menu to Save mode (for saving the game).
    /// </summary>
    void ShowSaveMode();

    /// <summary>
    /// Switches the menu to Load mode (for loading a game).
    /// </summary>
    void ShowLoadMode();
}
